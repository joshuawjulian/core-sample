# Personal Claude Code overrides (not team settings)
.claude/settings.local.json

# Quarto / build artifacts
/_book/
/.quarto/
/_site/
*_files/

# Rendered output (PDFs are built by CI; don't commit)
*.pdf

# LaTeX intermediates
*.aux
*.log
*.out
*.toc
*.synctex.gz

# OS / editor
.DS_Store
*~
